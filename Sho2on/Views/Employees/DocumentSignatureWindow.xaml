<!-- DocumentSignatureWindow.xaml -->
<Window x:Class="HR_Application.Views.DocumentSignatureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        FlowDirection="RightToLeft"
        Height="500" Width="600" WindowStartupLocation="CenterScreen"
        Background="WhiteSmoke">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2c3e50" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <TextBlock Text="توقيع الوثيقة" Foreground="White" FontSize="16" FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Document and Employee Info -->
        <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="الوثيقة:" FontWeight="Bold" Margin="0,0,10,0"/>
                <TextBlock Grid.Column="1" x:Name="txtDocumentTitle" FontWeight="Bold" Foreground="#2c3e50"/>

                <TextBlock Grid.Column="2" Text="الموظف:" FontWeight="Bold" Margin="20,0,10,0"/>
                <TextBlock Grid.Column="3" x:Name="txtEmployeeName" Foreground="#2c3e50"/>
            </Grid>
        </Border>

        <!-- Signature Form -->
        <Border Grid.Row="2" Background="White" CornerRadius="5" Padding="15">
            <StackPanel>
                <TextBlock Text="رفع الوثيقة الموقعة:" FontWeight="Bold" Margin="0,0,0,5"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <Button x:Name="browseSignedFileBtn" Content="اختر الملف الموقع" 
                            Click="browseSignedFileBtn_Click" Width="120" Margin="0,0,10,0"/>
                    <TextBlock x:Name="txtSignedFile" TextWrapping="Wrap" VerticalAlignment="Center"
                               Foreground="Gray" FontSize="12"/>
                </StackPanel>

                <TextBlock Text="ملاحظات:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtNotes" Height="80" TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" Margin="0,0,0,15"/>

                <TextBlock Text="سيتم حفظ الوثيقة الموقعة في أرشيف الموظف" 
                           FontSize="11" Foreground="Gray" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button x:Name="signBtn" Content="توقيع" Click="signBtn_Click" 
                    Width="100" Height="35" Background="#27ae60" Foreground="White" Margin="5"/>
            <Button x:Name="cancelBtn" Content="إلغاء" Click="cancelBtn_Click" 
                    Width="100" Height="35" Background="#95a5a6" Foreground="White" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>