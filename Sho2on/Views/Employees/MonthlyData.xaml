<Window x:Class="HR_Application.MonthlyData"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:uc="clr-namespace:HR_Application.UserControls"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        WindowState="Maximized"
        Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="Transparent">

    <Window.Resources>
        <!-- تدرج لوني للخلفية الرئيسية -->
        <LinearGradientBrush x:Key="MainBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#E3F2FD" Offset="0"/>
            <GradientStop Color="#BBDEFB" Offset="1"/>
        </LinearGradientBrush>
        <!-- تدرج للقسم العلوي -->
        <LinearGradientBrush x:Key="TopSectionBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#263238" Offset="0"/>
            <GradientStop Color="#37474F" Offset="1"/>
        </LinearGradientBrush>
        <!-- تأثير ظل -->
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="5" ShadowDepth="2" Color="Gray"/>
        <!-- نمط للحقول -->
        <Style x:Key="InputFieldStyle" TargetType="TextBox">
            <Setter Property="Width" Value="350"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
        <Style x:Key="WideComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
        <Style x:Key="UserComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <!-- نمط للأزرار -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="110"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1565C0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#0D47A1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="110"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#1976D2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#BBDEFB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- نمط للـ DataGrid -->
        <Style x:Key="MonthlyDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#1976D2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
        </Style>
        <!-- نمط لعناوين DataGrid -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <!-- نمط لصفوف DataGrid -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#1976D2"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="{StaticResource MainBackgroundBrush}"  >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="170"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- العنوان المحسن -->
            <TextBlock Text="الحركات الشهرية" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       FontSize="22" 
                       FontWeight="Bold" 
                       Foreground="#1976D2" 
                         
                       Grid.ColumnSpan="2"/>

            <!-- القسم العلوي المحسن -->
            <Border Background="{StaticResource TopSectionBrush}" FlowDirection="RightToLeft" Grid.Row="1" CornerRadius="0 0 20 20"  >
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- الفلاتر اليسرى -->
                    <StackPanel VerticalAlignment="Center" Orientation="Vertical" Margin="0 0 10 0">
                        <StackPanel Margin="5" Orientation="Horizontal" HorizontalAlignment="Center">
                            <Label Content="السنة" Foreground="#FFFFFF" HorizontalAlignment="Center"/>
                            <ComboBox  FlowDirection="LeftToRight" Name="year_box" Margin="5 0 0 0"/>
                        </StackPanel>
                        <StackPanel Margin="5" Orientation="Horizontal" HorizontalAlignment="Center">
                            <Label Content="الشهر" Foreground="#FFFFFF" HorizontalAlignment="Center"/>
                            <ComboBox FlowDirection="LeftToRight" Name="month_box" Margin="5 0 0 0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- الفلاتر الوسطى -->
                    <StackPanel VerticalAlignment="Center" Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Width="150" Tag="كود الموظف" VerticalAlignment="Bottom" FlowDirection="RightToLeft" Name="code_box" Margin="5"/>
                            <StackPanel Margin="5" Orientation="Vertical" HorizontalAlignment="Center">
                                <Label VerticalAlignment="Center" Content="الفرع" Foreground="#FFFFFF" />
                                <ComboBox Width="200" FlowDirection="LeftToRight" Name="branch_box" Margin="5 0 0 0"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel FlowDirection="LeftToRight" Orientation="Horizontal">
                            <ComboBox Width="250" SelectionChanged="userComboBox_SelectionChanged" Loaded="searchComboBox_Loaded" SelectedValuePath="Code" DisplayMemberPath="Name" HorizontalContentAlignment="Right" IsEditable="True" StaysOpenOnEdit="True" IsTextSearchEnabled="False" Name="user_box" Margin="5"/>
                            <Label Foreground="White" FontSize="14" FontWeight="Bold" Content="اسم الموظف" VerticalAlignment="Center" Margin="10 0 0 0"/>
                        </StackPanel>
                        <TextBox Visibility="Collapsed" Style="{StaticResource InputFieldStyle}" Tag="اسم الموظف" FlowDirection="RightToLeft" Name="name_box" Margin="5"/>
                    </StackPanel>

                    <!-- الأزرار -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0 0 0 0">
                        <StackPanel VerticalAlignment="Center" Orientation="Vertical" Margin="5">
                            <Button Style="{StaticResource SecondaryButtonStyle}" IsDefault="True" x:Name="data_show_btn" Click="data_show_btn_Click" Margin="5">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="Eye" VerticalAlignment="Center" Margin="0 0 5 0"/>
                                    <TextBlock Text="عرض البيانات" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource SecondaryButtonStyle}" x:Name="statistics_btn" Click="statistics_btn_Click" Margin="5">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="ChartBar" VerticalAlignment="Center" Margin="0 0 5 0"/>
                                    <TextBlock Text="الإحصائيات" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <StackPanel VerticalAlignment="Center" Orientation="Vertical" Margin="5">
                            <Button Style="{StaticResource SecondaryButtonStyle}" x:Name="print_btn" Click="print_btn_Click" Margin="5">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="Printer" VerticalAlignment="Center" Margin="0 0 5 0"/>
                                    <TextBlock Text="طباعة" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource SecondaryButtonStyle}" x:Name="excel_btn" Click="excel_btn_Click" Margin="5">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="FileExcel" VerticalAlignment="Center" Margin="0 0 5 0"/>
                                    <TextBlock Text="Excel" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- DataGrid المحسن -->
            <Border Grid.Row="2" Background="#FFFFFF" Opacity="0.95"   Margin="20">
                <DataGrid Style="{StaticResource MonthlyDataGridStyle}" ItemsSource="{Binding MonthDatas, UpdateSourceTrigger=PropertyChanged}" LoadingRow="AttendanceDataGrid_LoadingRow" CellEditEnding="DataGrid_CellEditEnding" AutoGenerateColumns="False" Name="dataGrid" Margin="10" FlowDirection="RightToLeft">
                    <DataGrid.Resources>
                        <ContextMenu x:Key="RowContextMenu">
                            <MenuItem Header="غياب" Click="MakeAbsence_Click"/>
                            <MenuItem Header="إلغاء غياب" Click="CancelAbsence_Click"/>
                            <MenuItem Header="إجراء" Click="MakeDeduction_Click"/>
                            <MenuItem Header="تغيير الوردية" Click="ChangeShift_Click"/>
                            <MenuItem Header="إجازة" Click="MakeHoliday_Click"/>
                        </ContextMenu>
                    </DataGrid.Resources>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="ContextMenu" Value="{StaticResource RowContextMenu}"/>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn IsReadOnly="True" CanUserSort="False" Header="اليوم" Binding="{Binding Day}" Width="80"/>
                        <DataGridTextColumn IsReadOnly="True" CanUserSort="False" Header="Holiday" Binding="{Binding isHoliday}" Width="80" Visibility="Hidden"/>
                        <DataGridTextColumn IsReadOnly="True" CanUserSort="False" Header="Absence" Binding="{Binding isAbsence}" Width="80" Visibility="Hidden"/>
                        <DataGridTextColumn IsReadOnly="True" CanUserSort="False" Header="التاريخ" Binding="{Binding Date, StringFormat={}{0:yyyy/MM/dd}}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FlowDirection" Value="LeftToRight"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn IsReadOnly="False" Header="حضور" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Center" FlowDirection="LeftToRight" Text="{Binding AttendanceTime, StringFormat=\{0:HH\\:mm\\:ss\}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Height="25" Margin="0" Width="100" HorizontalAlignment="Center" FlowDirection="LeftToRight" Text="{Binding AttendanceTime, StringFormat=\{0:HH\\:mm\\:ss\}, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn IsReadOnly="True" CanUserSort="False" Header="موقع الحضور" Binding="{Binding AttendBranch}"/>
                        <DataGridTemplateColumn IsReadOnly="False" Header="انصراف" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Center" FlowDirection="LeftToRight" Text="{Binding DepartureTime, StringFormat=\{0:HH\\:mm\\:ss\}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Height="25" Margin="0" Width="100" HorizontalAlignment="Center" FlowDirection="LeftToRight" Text="{Binding DepartureTime, UpdateSourceTrigger=PropertyChanged, StringFormat=\{0:HH\\:mm\\:ss\}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn IsReadOnly="True" CanUserSort="False" Header="موقع الانصراف" Binding="{Binding DepartBranch}"/>
                        <DataGridCheckBoxColumn Binding="{Binding ExemptLate, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                            <DataGridCheckBoxColumn.Header>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Unchecked="allLate_check_Unchecked" Checked="allLate_check_Checked" Name="allLate_check" Content="معفي تأخير" Foreground="White" VerticalContentAlignment="Center"/>
                                </StackPanel>
                            </DataGridCheckBoxColumn.Header>
                        </DataGridCheckBoxColumn>
                        <DataGridCheckBoxColumn Visibility="Collapsed" Binding="{Binding ExemptINEarly, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                            <DataGridCheckBoxColumn.Header>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Unchecked="allINEarly_check_Unchecked" Checked="allINEarly_check_Checked" Name="allINEarly_check" Content="معفي د مبكر" Foreground="White" VerticalContentAlignment="Center"/>
                                </StackPanel>
                            </DataGridCheckBoxColumn.Header>
                        </DataGridCheckBoxColumn>
                        <DataGridCheckBoxColumn Visibility="Collapsed" Binding="{Binding ExemptEarly, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                            <DataGridCheckBoxColumn.Header>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Unchecked="allEarly_check_Unchecked" Checked="allEarly_check_Checked" Name="allEarly_check" Content="معفي خ مبكر" Foreground="White" VerticalContentAlignment="Center"/>
                                </StackPanel>
                            </DataGridCheckBoxColumn.Header>
                        </DataGridCheckBoxColumn>
                        <DataGridCheckBoxColumn Binding="{Binding ExemptOT, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                            <DataGridCheckBoxColumn.Header>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Unchecked="allOT_check_Unchecked" Checked="allOT_check_Checked" Name="allOT_check" Content="معفي الاضافي" Foreground="White" VerticalContentAlignment="Center"/>
                                </StackPanel>
                            </DataGridCheckBoxColumn.Header>
                        </DataGridCheckBoxColumn>
                        <DataGridTemplateColumn IsReadOnly="True" Header="الوردية" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Center" FlowDirection="LeftToRight" Text="{Binding Shift}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn IsReadOnly="True" CanUserSort="False" Header="س العمل" Binding="{Binding WorkHours, StringFormat={}{0:hh\\:mm}}" Width="100"/>
                        <DataGridTextColumn IsReadOnly="True" CanUserSort="False" Header="التأخير" Binding="{Binding Late, StringFormat={}{0:hh\\:mm}}" Width="100"/>
                        <DataGridTextColumn IsReadOnly="True" CanUserSort="False" Header="الاضافي" Binding="{Binding OverTime, StringFormat={}{0:hh\\:mm}}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- الصورة المتحركة للتحميل -->
            <Image x:Name="loadingProgressBar" 
                   Source="pack://application:,,,/assets/images/loading.png" 
                   Grid.Row="2"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   Width="0" 
                   Height="0">
                <Image.RenderTransform>
                    <RotateTransform x:Name="rotateTransform" CenterX="25" CenterY="25"/>
                </Image.RenderTransform>
                <Image.Triggers>
                    <EventTrigger RoutedEvent="Image.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation 
                                 Storyboard.TargetName="rotateTransform" 
                                 Storyboard.TargetProperty="Angle" 
                                 From="0" 
                                 To="350" 
                                 Duration="0:0:1"
                                 RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Image.Triggers>
            </Image>
        </Grid>
    </Border>
</Window>