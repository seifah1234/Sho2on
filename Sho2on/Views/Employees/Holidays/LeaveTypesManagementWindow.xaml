<Window x:Class="HR_Application.Views.Employees.Holidays.LeaveTypesManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="{DynamicResource ManagingLeaveTypes}" 
        Height="600" Width="800"
        FlowDirection="RightToLeft"
        Background="{DynamicResource WindowRootBackground}"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- شريط الأدوات -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnAdd" Content="{DynamicResource AddNew}" Width="120" Height="35" Click="btnAdd_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#4CAF50" CornerRadius="5">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0">
                                <Icon:PackIconMaterial Kind="Plus" Foreground="White" Width="16" Height="16"/>
                                <TextBlock Text=" إضافة جديد" Foreground="White" Margin="5,0,0,0"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>

        <!-- جدول أنواع الإجازات -->
        <DataGrid x:Name="dgLeaveTypes" 
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  RowHeight="35"
                  CellStyle="{DynamicResource DataGridCellStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource Code}" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="{DynamicResource Type}" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="{DynamicResource DefaultBalance}" Binding="{Binding DefaultBalance}" Width="120"/>
                <DataGridCheckBoxColumn Header="{DynamicResource DeductedFromTheBalance}" Binding="{Binding DeductFromBalance}" Width="120">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="FlowDirection" Value="LeftToRight"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>
                <DataGridCheckBoxColumn Header="{DynamicResource RequiresApproval}" Binding="{Binding RequiresApproval}" Width="120">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="FlowDirection" Value="LeftToRight"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>
                <DataGridTextColumn Header="{DynamicResource MaximumConsecutiveDuration}" Binding="{Binding MaxConsecutiveDays}" Width="120"/>
                <DataGridCheckBoxColumn Header="{DynamicResource Active}" Binding="{Binding IsActive}" Width="80">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="FlowDirection" Value="LeftToRight"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>

                <DataGridTemplateColumn Header="{DynamicResource Procedures}" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="btnEdit" Tag="{Binding Id}" Click="btnEdit_Click" 
                                        ToolTip="{DynamicResource Amendment}" Width="30" Height="30">
                                    <Icon:PackIconMaterial Kind="Pencil" Width="16" Height="16"/>
                                </Button>
                                <Button x:Name="btnDelete" Tag="{Binding Id}" Click="btnDelete_Click" 
                                        ToolTip="{DynamicResource Delete}" Width="30" Height="30" Margin="5,0,0,0">
                                    <Icon:PackIconMaterial Kind="Delete" Width="16" Height="16"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- أزرار الإغلاق -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="btnClose" Content="{DynamicResource Closing}" Width="80" Height="35" Click="btnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>