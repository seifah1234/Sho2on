<Window x:Class="HR_Application.LoginScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:uc="clr-namespace:HR_Application.UserControls"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Height="550" Width="900" MinHeight="550" MinWidth="950" 
        WindowStyle="SingleBorderWindow" 
        FlowDirection="RightToLeft"
        WindowStartupLocation="CenterScreen" 
        Background="Transparent">

    <Window.Resources>
        <!-- تدرج لوني للخلفية الرئيسية -->
        <LinearGradientBrush x:Key="MainBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#E3F2FD" Offset="0"/>
            <GradientStop Color="#BBDEFB" Offset="1"/>
        </LinearGradientBrush>
        <!-- تأثير ظل -->
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="5" ShadowDepth="2" Color="Gray"/>
        <!-- نمط للحقول -->
        <Style x:Key="InputFieldStyle" TargetType="TextBox">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#1976D2"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#424242"/>
        </Style>
        <Style x:Key="PasswordFieldStyle" TargetType="PasswordBox">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#1976D2"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#424242"/>
        </Style>
        <!-- نمط للزر -->
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1565C0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#0D47A1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="{StaticResource MainBackgroundBrush}"  >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- العنوان المحسن -->
            <TextBlock Text="Welcome to Sho2on" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       FontSize="22" 
                       FontWeight="Bold" 
                       Foreground="#1976D2" 
                         
                       Grid.ColumnSpan="2"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- المنطقة الرئيسية -->
                <Border CornerRadius="20">
                    <Grid>

                        <StackPanel VerticalAlignment="Center" Orientation="Vertical" HorizontalAlignment="Center">
                            <!-- حقول الإدخال مع أيقونات -->
                            <StackPanel HorizontalAlignment="Center" Orientation="Vertical" Margin="10">
                                <!-- Username -->
                                <TextBlock HorizontalAlignment="Right" Text="Username" Foreground="#1976D2" FontSize="14" FontWeight="Bold" Margin="0 0 0 5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="5 0 0 5" BorderBrush="#1976D2" BorderThickness="2 2 0 2">
                                        <Icon:PackIconMaterial Kind="Account" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#1976D2" Width="20" Height="20"/>
                                    </Border>
                                    <TextBox Grid.Column="1" Style="{StaticResource InputFieldStyle}" Name="username_box" Focusable="True" BorderThickness="2"  TabIndex="1"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel HorizontalAlignment="Center" Orientation="Vertical" Margin="10">
                                <!-- Password -->
                                <TextBlock HorizontalAlignment="Right" Text="Password"  Foreground="#1976D2" FontSize="14" FontWeight="Bold" Margin="0 0 0 5"/>
                                <Grid >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="5 0 0 5" BorderBrush="#1976D2" BorderThickness="2 2 0 2">
                                        <Icon:PackIconMaterial Kind="Lock" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#1976D2" Width="20" Height="20"/>
                                    </Border>
                                    <PasswordBox Grid.Column="1" FlowDirection="LeftToRight" Style="{StaticResource PasswordFieldStyle}" Name="pass_box" BorderThickness="2" TabIndex="2"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel HorizontalAlignment="Center" Orientation="Vertical" Margin="10">
                                <!-- Database Host -->
                                <TextBlock HorizontalAlignment="Right" Text="Database Host" Foreground="#1976D2" FontSize="14" FontWeight="Bold" Margin="0 0 0 5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="5 0 0 5" BorderBrush="#1976D2" BorderThickness="2 2 0 2">
                                        <Icon:PackIconMaterial Kind="Server" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#1976D2" Width="20" Height="20"/>
                                    </Border>
                                    <TextBox Grid.Column="1" Style="{StaticResource InputFieldStyle}" Name="IPDB_box" BorderThickness="2"  TabIndex="3"/>
                                </Grid>
                            </StackPanel>

                            <!-- زر تسجيل الدخول المحسن -->
                            <Border HorizontalAlignment="Center" Name="btn_border" Margin="20" CornerRadius="8" Background="#1976D2" Cursor="Hand"  >
                                <Button Name="login_btn" Click="login_Clicked" Content="تسجيل الدخول" IsDefault="True" Style="{StaticResource LoginButtonStyle}" BorderThickness="0"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <Image Source="/assets/images/Background.gif" Name="GIFImage" Grid.Column="1" Stretch="UniformToFill"/>
            </Grid>
        </Grid>
    </Border>
</Window>