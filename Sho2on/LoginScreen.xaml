<Window x:Class="HR_Application.LoginScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:uc="clr-namespace:HR_Application.UserControls"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Height="550" Width="900" MinHeight="550" MinWidth="950" 
        WindowStyle="SingleBorderWindow" 
        FlowDirection="RightToLeft"
        WindowStartupLocation="CenterScreen" 
        Background="Transparent">

    <Window.Resources>
        
        <!-- تأثير ظل -->
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="5" ShadowDepth="2" Color="Gray"/>
        <!-- نمط للحقول -->
        <Style x:Key="InputFieldStyle" TargetType="TextBox">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="{StaticResource ThirdColor}"/>
            <Setter Property="Foreground" Value="#424242"/>
        </Style>
        <Style x:Key="PasswordFieldStyle" TargetType="PasswordBox">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="{StaticResource ThirdColor}"/>
            <Setter Property="Foreground" Value="#424242"/>
        </Style>
        <!-- نمط للزر -->
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="{StaticResource ThirdColor}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#0D47A1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="{StaticResource MainBackgroundBrush}"  >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- العنوان المحسن -->
            <TextBlock Text="Welcome to Sho2on" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       FontSize="22" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource PrimaryColor}" 
                         
                       Grid.ColumnSpan="2"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- المنطقة الرئيسية -->
                <Border CornerRadius="20" Background="{StaticResource PrimaryColor}">
                    <Grid>

                        <StackPanel VerticalAlignment="Center" Orientation="Vertical" HorizontalAlignment="Center">
                            <!-- حقول الإدخال مع أيقونات -->
                            <StackPanel HorizontalAlignment="Center" Orientation="Vertical" Margin="10">
                                <!-- Username -->
                                <TextBlock HorizontalAlignment="Right" Text="Username" Foreground="{StaticResource ThirdColor}" FontSize="14" FontWeight="Bold" Margin="0 0 0 5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="{StaticResource ThirdColor}" CornerRadius="5 0 0 5" BorderBrush="{StaticResource PrimaryColor}" BorderThickness="2 2 0 2">
                                        <Icon:PackIconMaterial Kind="Account" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryColor}" Width="20" Height="20"/>
                                    </Border>
                                    <TextBox Grid.Column="1" Style="{StaticResource InputFieldStyle}" Name="username_box" Focusable="True" BorderThickness="2"  TabIndex="1"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel HorizontalAlignment="Center" Orientation="Vertical" Margin="10">
                                <!-- Password -->
                                <TextBlock HorizontalAlignment="Right" Text="Password"  Foreground="{StaticResource ThirdColor}" FontSize="14" FontWeight="Bold" Margin="0 0 0 5"/>
                                <Grid >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="{StaticResource ThirdColor}" CornerRadius="5 0 0 5" BorderBrush="{StaticResource PrimaryColor}" BorderThickness="2 2 0 2">
                                        <Icon:PackIconMaterial Kind="Lock" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryColor}" Width="20" Height="20"/>
                                    </Border>
                                    <PasswordBox Grid.Column="1" FlowDirection="LeftToRight" Style="{StaticResource PasswordFieldStyle}" Name="pass_box" BorderThickness="2" TabIndex="2"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel HorizontalAlignment="Center" Orientation="Vertical" Margin="10">
                                <!-- Database Host -->
                                <TextBlock HorizontalAlignment="Right" Text="Database Host" Foreground="{StaticResource ThirdColor}" FontSize="14" FontWeight="Bold" Margin="0 0 0 5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="{StaticResource ThirdColor}" CornerRadius="5 0 0 5" BorderBrush="{StaticResource PrimaryColor}" BorderThickness="2 2 0 2">
                                        <Icon:PackIconMaterial Kind="Server" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryColor}" Width="20" Height="20"/>
                                    </Border>
                                    <TextBox Grid.Column="1" Style="{StaticResource InputFieldStyle}" Name="IPDB_box" BorderThickness="2"  TabIndex="3"/>
                                </Grid>
                            </StackPanel>

                            <!-- زر تسجيل الدخول المحسن -->
                            <Border HorizontalAlignment="Center" Name="btn_border" Margin="20" CornerRadius="8" Background="#1976D2" Cursor="Hand"  >
                                <Button Name="login_btn" Click="login_Clicked" Content="تسجيل الدخول" IsDefault="True" Style="{StaticResource LoginButtonStyle}" BorderThickness="0"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <Image Source="/assets/images/Background.gif" Name="GIFImage" Grid.Column="1" Stretch="UniformToFill"/>
            </Grid>
        </Grid>
    </Border>
</Window>